@model PINrad.Models.ErrorViewModel

@{
    ViewData["Title"] = "Error";
}

<h1 class="text-danger">Došlo je do pogreške.</h1>
<h2 class="text-danger">Molimo pokušajte ponovno kasnije.</h2>

@if (Model.ShowRequestId)
{
    <p>
        <strong>Request ID:</strong> <code>@Model.RequestId</code>
    </p>
}
